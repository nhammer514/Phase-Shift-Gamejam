// Breathing animation settings
breathe_timer   = 0;      // animation phase
breathe_speed   = .125;      // cycles per second
breathe_amp     = 0.10;   // scale amplitude 
base_scale_x    = 3;      // original x scale of text
base_scale_y    = 3;      // original y scale of text
vertical_amp_px = 6;      // vertical bob amplitude (px)

// Fade settings
fade_min_alpha  = 0.8;    // lowest opacity (0 = invisible, 1 = solid)
fade_max_alpha  = 1.0;    // highest opacity
